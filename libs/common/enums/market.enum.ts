export enum MarketType {
  Over = 'OVER',
  Under = 'UNDER',
  OverUnder = 'OVER_UNDER',
  HandiCap = 'HANDICAP',
  WinOrLose = 'WIN_LOSE',
  Other = 'OTHER',
}

export enum MarketStatus {
  Deactivate = 0,
  Active = 1,
  Hide = 2,
}
