export enum EConnStatusTab {
  Default = 'default',
  OperatingSystem = 'operatingSystem',
  Browser = 'browser',
  Hour = 'hour',
  Day = 'day',
  Date = 'date',
  Month = 'month',
  Year = 'year',
}
