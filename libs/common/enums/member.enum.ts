export enum GenderEnum {
  Male = 'MALE',
  Female = 'FEMALE',
  Other = 'OTHER',
}

export enum MemoNoteType {
  Note = 'NOTE',
  Money = 'MONEY',
}

export enum MemberFilterDate {
  CreatedAt = 'createdAt',
  UpdatedAt = 'updatedAt',
  LastAccess = 'lastAccess',
}
